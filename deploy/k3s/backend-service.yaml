apiVersion: v1
kind: Service
metadata:
  name: nexora-backend
  namespace: nexora
  labels:
    app: nexora-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: nexora-backend
