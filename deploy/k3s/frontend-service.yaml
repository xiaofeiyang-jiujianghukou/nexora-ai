apiVersion: v1
kind: Service
metadata:
  name: nexora-frontend
  namespace: nexora
  labels:
    app: nexora-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: nexora-frontend
