# Nexora AI — Prometheus 监控配置（精简版）
global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "nexora-backend"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["host.docker.internal:8080"]
